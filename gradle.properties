android.useAndroidX=true

# AppCat Android SDK version. Both the Maven publication and JitPack
# pick this up via build.gradle's `project.findProperty('VERSION_NAME')`.
# Bump together with the git tag when cutting a release.
VERSION_NAME=0.1.0
